import AppShell from '../../components/AppShell';

export default function SettingsPage() {
  return (
    <AppShell title="Settings">
      <section className="card">
        <p className="muted">
          Settings will live here once profile preferences are wired up.
        </p>
      </section>
    </AppShell>
  );
}
